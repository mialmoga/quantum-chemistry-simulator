{
  "id": "specular_metal",
  "name": "Metal Especular",
  "stage": "frag",
  "target": [
    "orbital",
    "sphere"
  ],
  "color": "#e2e8f0",
  "desc": "Simula brillo metálico/mercurio. Requiere normales o vPhase para calcular el reflejo.",
  "sub": "col += pow(dot(N, L), exp) * metal_color",
  "requires": [
    "brightness"
  ],
  "provides": [],
  "params": {
    "shininess": {
      "label": "Brillo/Dureza",
      "min": 2.0,
      "max": 128.0,
      "step": 1.0,
      "val": 64.0
    },
    "intensity": {
      "label": "Intensidad",
      "min": 0.0,
      "max": 5.0,
      "step": 0.1,
      "val": 2.5
    },
    "reflectivity": {
      "label": "Reflectividad",
      "min": 0.0,
      "max": 1.0,
      "step": 0.05,
      "val": 0.8
    }
  },
  "glsl": "{ vec3 lightDir = normalize(vec3(0.5, 1.0, 0.5)); vec3 viewDir = vec3(0.0, 0.0, 1.0); vec3 normal = normalize(vWorldNormal); float spec = pow(max(dot(reflect(-lightDir, normal), viewDir), 0.0), {{shininess}}); vec3 metalCol = vec3(0.9, 0.94, 1.0); col = mix(col, metalCol, spec * {{reflectivity}}); col += spec * {{intensity}}; }"
}
